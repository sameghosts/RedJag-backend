// Dependencies

const mongoose =require('mongoose');

// Instantiate Schema
const Schema = mongoose.Schema;

//Options
const options = {
  timestamps: true
}

// m TodoListItem Schema
const todoListItemSchema = new Schema ({
  
})

// Make TodoList Schema
const todoListSchema = new Schema ({

})

const todoCollection = new Schema ({
  
})